export type Banner = {
  image: string;
  category: string;
};

export type Thumbnail = {
  category: string;
  image: string;
  nickname: string;
  quizLike: string;
  viewCount: string;
  title: string;
};
